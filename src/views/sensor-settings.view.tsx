import { Text, View } from 'react-native';

export const SensorSettingsView = ({}) => {
    return (
        <View
            style={{
                display: 'flex',
                flex: 1,
                alignItems: 'center',
                justifyContent: 'center',
            }}
        >
            <Text>SensorSettingsView</Text>
        </View>
    );
};

export default SensorSettingsView;
